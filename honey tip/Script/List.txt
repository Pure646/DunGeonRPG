선언

List<int> numbers = new List<int>(); 		// int형 숫자 리스트
List<string> names = new List<string>(); 	// string 리스트


메서드/속성		설명

Add(item)			값 추가
Insert(index, item)		특정 위치에 값 삽입
Remove(item)		값 제거
RemoveAt(index)		해당 위치 값 제거
Clear()			모든 값 제거
Count			요소 개수
Contains(item)		포함 여부 확인
IndexOf(item)		값의 위치 반환
Sort()			정렬