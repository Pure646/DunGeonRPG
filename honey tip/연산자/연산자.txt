1. | (OR 연산자): 두 값을 비교하여 하나라도 1인 자리를 1로 만듭니다. 플래그를 조합할 때 사용합니다.

2. & (AND 연산자): 두 값을 비교하여 모두 1인 자리를 1로 만듭니다. 특정 플래그를 제거할 때 사용합니다.

3. ^ (XOR 연산자): 두 값이 다르면 1, 같으면 0인 자리를 1로 만듭니다. 특정 플래그를 토글할 때 사용합니다.

4. ~ (NOT 연산자): 값을 반전시킵니다. 즉, 1은 0으로, 0은 1로 바꿔서 반대 값을 구할 때 사용합니다.